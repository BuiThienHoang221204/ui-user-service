# Same-origin proxy path to avoid browser CORS preflight on bearer-auth requests
VITE_API_BASE_URL=/api-user

# Optional dev-only upstream target for the Vite proxy
VITE_API_GATEWAY_URL=https://igkhi0x0hd.execute-api.ap-southeast-1.amazonaws.com
